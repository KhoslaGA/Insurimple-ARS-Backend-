# Postgres connection. The app connects as a NON-owner role so FORCE ROW LEVEL
# SECURITY applies to it (the owner bypasses RLS). Provision the role/db first.
DATABASE_URL="postgresql://app:app@localhost:5432/insurimple_ars?schema=public"

# Port the API listens on.
PORT=4000
